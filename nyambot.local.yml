ports:
  backend:
    host: 127.0.0.1
    port: 8000
    command: "APP_ENV=dev python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8000"
  database:
    host: 127.0.0.1
    port: 15432
    docker_compose_file: docker-compose.pgvector.yml
