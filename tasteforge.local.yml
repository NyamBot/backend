ports:
  backend:
    host: 127.0.0.1
    port: 8000
    command: ".\\.venv-win\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000"
  database:
    host: 127.0.0.1
    port: 15432
    docker_compose_file: docker-compose.pgvector.yml

