apiVersion: v1
kind: Service
metadata:
  name: nyambot-backend
  labels:
    app.kubernetes.io/name: nyambot-backend
    app.kubernetes.io/part-of: nyambot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nyambot-backend
  ports:
    - name: http
      port: 80
      targetPort: http
